export interface TopClientResponse {
  name: string;
  surname: string;
  total: number;
}

export interface TopPropertyResponse {
  address: string;
  total: number;
}

export interface TopRealEstateResponse {
  name: string;
  total: number;
}